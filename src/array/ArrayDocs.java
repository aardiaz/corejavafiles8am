package array;

public class ArrayDocs {
	
	/*
	 * ------------- Array -----------------
	 *   # use to store and processing static multiple data.
	 *   # collection of homogeneous data.
	 *   # collection/group of multiple values of same data type with fixed size.  
	 *   # data stored on the basis of indices (index starts from 0)
	 *   # contains duplicate data.
	 *   # random access.
	 *   # contiguous memory allocation.
	 *   # array it self is an object in java.
	 *   
	 *   syntax :
	 *   
	 *    data_type   array_name[] = new  data_type[size];
	 *   
	 *    e.g.
	 *    
	 *    String   names[] = new String[200];
	 *    char[]   alphs = new  char[26];
	 *    float   []heights = new  float[5000];
	 *    
	 *    Book  books[] =  new Book[500];
	 *    Employee  emps[] =  new Employee[80000];
	 *    
	 *    
	 */

}
